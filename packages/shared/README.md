# @openviz/shared

Shared TypeScript contracts and browser-safe cross-package modules used by frontend, backend, and worker code paths.

Current scope:

- Shared data and chart contracts under `src/types`
